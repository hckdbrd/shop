package com.project.shop.repository;

import com.project.shop.entity.DeviceCase;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DeviceCaseRepository extends JpaRepository<DeviceCase, Long> {
}
